--#!/usr/bin/env lua

--require('os')

--local DnDBase = require('devinanddell_base')

--local lu = require('luaunit')

io.write("\nSTART..........\n")

--local status, err = pcall(lu.assertTrue( true ))
--print(err.code)
--print(status)

--lu.assertFalse( false )
--lu.assertEvalToFalse( nil )
--lu.assertFalse( nil )

--lu.assertNotEquals("apple", 1)

--lu.assertTrue( DnDBase.HasFreeSpinsRemaining() )
--lu.assertFalse( DnDBase.HasFreeSpinsRemaining() )

--lu.assertEquals(true, DnDBase.HasFreeSpinsRemaining() == true)

--lu.assertEquals(false, DnDBase.HasFreeSpinsRemaining() == true)

--assert(true == DnDBase.HasFreeSpinsRemaining())
--assert(false == DnDBase.HasFreeSpinsRemaining())

--local runner = lu.LuaUnit.new()
--runner:setOutputType("junit", "junit_xml_file")
--os.exit( runner:runSuite() )

io.write("\nEND............\n")


